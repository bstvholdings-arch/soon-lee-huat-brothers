# SQL 粘贴 Supabase 报错 — 根因与修复 (2026-09-06)

## 根因
1. `supabase/run_all.sql`(我之前合并的文件)里混入了 `//` 注释分隔行。
   PostgreSQL 只认 `--` 和 `/* */`,**不认 `//`**,所以整段粘贴到 SQL Editor 直接 syntax error。
2. `set_company_reg.sql` 原文件带 UTF-8 BOM(ï»¿),部分解析器首行报错。

## 修复
- 已告知用户**停止使用 run_all.sql**,改用 4 个独立干净文件分别粘贴:
  - supabase/schema.sql
  - supabase/add_api_keys.sql
  - supabase/add_orders_and_sku.sql
  - set_company_reg.sql
- 已用 `UTF8Encoding(false)` 重写 set_company_reg.sql,去除 BOM。现 4 个文件均:无 BOM、无 `//`、幂等。
- 验证:4 文件 `//` 行数=0;schema/api_keys/orders/reg 标记齐全。

## 结论
磁盘 4 个源文件本身一直干净(只有 run_all.sql 和我聊天里贴的合并版有污染)。用户改用 4 独立文件后粘贴成功(用户回「好了」)。

## 教训
- 合并 SQL 时分隔注释必须用 `--`,严禁 `//`。
- 写 SQL 文件用无 BOM UTF-8。
- 用户粘贴失败时,最快定位方式是贴 ERROR 文字;本例靠静态检查(// 检测 + BOM 检测)定位。

## 之后待确认/待办
- 跑完应在后台看到 Orders 菜单、首页 Hero 显示注册号 201901003575 (1312901-T)。
- .env.local 补 WHATSAPP_TOKEN / WHATSAPP_PHONE_NUMBER_ID(WhatsApp 告警才生效)。
- 建议 Roll 一次 service_role key。
